# 言道学外语 - 项目完整打包指南

## 📋 目录

1. [项目现状检查](#项目现状检查)
2. [数据库设置](#数据库设置)
3. [Web应用构建](#web应用构建)
4. [APK打包](#apk打包)
5. [部署到腾讯云COS](#部署到腾讯云cos)
6. [小程序配置](#小程序配置)
7. [最终检查清单](#最终检查清单)

---

## ✅ 项目现状检查

### 当前项目位置
```
C:\youdao-main (1)\youdao-main\
```

### 项目文件结构
```
youdao-main/
├── public/                     # 公共资源
│   ├── icon.svg              # 🦜 绿色鹦鹉图标（已更新）
│   ├── vite.svg              # 图标
│   ├── download.html         # APP下载页面（已创建）
│   └── audio/                # 音频资源
├── src/
│   ├── components/           # 所有组件（完整）
│   │   ├── AIAssistant.tsx
│   │   ├── AchievementWall.tsx
│   │   ├── DailyTasks.tsx
│   │   ├── FriendSystem.tsx
│   │   ├── PhoneVerify.tsx
│   │   └── ... (40+ 组件)
│   ├── data/                # 数据文件
│   │   ├── languages.ts
│   │   ├── dynamic_content.json
│   │   └── massive_content.json  # 📚 海量内容
│   ├── lib/                 # 工具库
│   ├── App.tsx              # 主应用
│   └── main.tsx
├── supabase/                # 数据库迁移文件（20+ 个）
├── 最终完整数据库.sql       # 🗄️ 完整数据库脚本（已创建）
├── package.json
├── vite.config.ts
└── capacitor.config.json    # Capacitor配置
```

### 功能检查清单
- ✅ 用户认证系统（手机验证码）
- ✅ 成就系统（15个成就）
- ✅ 好友系统
- ✅ 每日任务和打卡
- ✅ AI对话助手（4种角色）
- ✅ 学习游戏（5种游戏）
- ✅ 语法词汇学习
- ✅ 虚拟电台
- ✅ 会员中心和商店
- ✅ 4500+ 海量内容（笑话、童谣、小说）
- ✅ 绿色鹦鹉图标（已更新）
- ✅ 应用名称（言道学外语）

---

## 🗄️ 数据库设置

### 步骤1: 打开Supabase控制台
1. 访问 https://supabase.com/dashboard
2. 选择您的项目

### 步骤2: 执行SQL脚本
在Supabase的SQL编辑器中，执行以下文件（按顺序）：

**文件位置：** `C:\youdao-main (1)\youdao-main\最终完整数据库.sql`

**这个脚本包含：**
- ✅ 所有表结构
- ✅ 10种语言配置
- ✅ 15个成就定义
- ✅ 笑话分类和示例内容
- ✅ 童谣和短篇小说
- ✅ 电台内容
- ✅ 商店商品
- ✅ 所有索引和约束

**执行方法：**
1. 在Supabase中点击「SQL Editor」
2. 点击「New Query」
3. 打开 `最终完整数据库.sql` 文件
4. 复制全部内容粘贴到编辑器
5. 点击「Run」执行

---

## 🌐 Web应用构建

### 步骤1: 安装依赖
打开命令行（CMD或PowerShell），执行：
```cmd
cd C:\youdao-main (1)\youdao-main
npm install
```

### 步骤2: 构建Web应用
```cmd
npm run build
```

构建成功后，会生成 `dist/` 文件夹，包含完整的Web应用。

### 步骤3: 本地预览（可选）
```cmd
npm run preview
```

访问 http://localhost:4173 查看效果。

---

## 📱 APK打包

### 前置条件
- ✅ Node.js 16+
- ✅ Java 17（推荐，或Java 21）
- ✅ Android SDK（如果没有，会自动下载）

### 方案A: 完整APK打包（推荐）

#### 步骤1: 初始化Capacitor（如果还没）
```cmd
npm install @capacitor/core @capacitor/cli @capacitor/android --save-dev
npx cap init "言道学外语" "com.yandao.app"
```

#### 步骤2: 添加Android平台
```cmd
npx cap add android
```

#### 步骤3: 配置Gradle镜像（解决下载超时）

**修改文件：** `android/gradle/wrapper/gradle-wrapper.properties`
```properties
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
# 改为阿里云镜像
distributionUrl=https\://mirrors.aliyun.com/macports/distfiles/gradle/gradle-7.5-all.zip
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
networkTimeout=120000
```

**修改文件：** `android/build.gradle`
在 `buildscript` 和 `allprojects` 中添加阿里云镜像：
```gradle
buildscript {
    repositories {
        maven { url 'https://maven.aliyun.com/repository/google' }
        maven { url 'https://maven.aliyun.com/repository/jcenter' }
        maven { url 'https://maven.aliyun.com/repository/public' }
    }
}

allprojects {
    repositories {
        maven { url 'https://maven.aliyun.com/repository/google' }
        maven { url 'https://maven.aliyun.com/repository/jcenter' }
        maven { url 'https://maven.aliyun.com/repository/public' }
    }
}
```

**修改文件：** `android/app/build.gradle`
确保Java兼容性：
```gradle
compileOptions {
    sourceCompatibility JavaVersion.VERSION_1_8
    targetCompatibility JavaVersion.VERSION_1_8
}
```

#### 步骤4: 同步并构建
```cmd
npm run build
npx cap copy
cd android
gradlew assembleRelease
```

#### 步骤5: 获取APK
APK文件位置：
```
android/app/build/outputs/apk/release/app-release.apk
```

重命名为 `yandao.apk`

---

### 方案B: 跳过APK，先上线Web版

如果APK打包遇到问题，可以：
1. 先完成Web应用部署
2. 用户可以通过Web版使用
3. 后续再解决APK打包问题

---

## ☁️ 部署到腾讯云COS

### 步骤1: 准备文件
需要上传到COS的文件：
1. `dist/` 文件夹的所有内容
2. `public/download.html`（放在根目录）
3. `yandao.apk`（放在 `download/` 目录）

### 步骤2: 上传到COS

**方法1: 手动上传（推荐，最简单）**
1. 登录腾讯云控制台
2. 进入「对象存储 COS」
3. 选择您的存储桶（yandao）
4. 点击「上传文件」
5. 上传以下内容：
   - `dist/` 里的所有文件 → 存储桶根目录
   - `public/download.html` → 存储桶根目录
   - `yandao.apk` → 创建 `download/` 文件夹，上传到这里

**方法2: 使用腾讯云CLI**
如果已安装CLI，可以用命令上传。

### 步骤3: 配置静态网站
1. 在COS存储桶设置中
2. 找到「静态网站」
3. 开启静态网站
4. 索引文档设置为 `index.html`
5. 错误文档设置为 `index.html`

### 步骤4: 配置自定义域名（可选）
1. 在「自定义域名」中添加 `yandao.vip`
2. 申请免费SSL证书
3. 开启强制HTTPS

### 最终访问地址
- Web应用: https://yandao.vip
- 下载页面: https://yandao.vip/download.html
- APK下载: https://yandao.vip/download/yandao.apk

---

## 📱 小程序配置

### 小程序项目位置
```
C:\youdao-main (1)\youdao-main\miniprogram\
```

### 导入步骤
1. 打开微信开发者工具
2. 选择「导入项目」
3. 项目目录选择 `miniprogram` 文件夹
4. AppID选择「使用测试号」或您的AppID
5. 项目名称：言道学外语
6. 点击「导入」

### 常见问题解决

**问题1: tabBar图标找不到**
- 解决：在 `app.json` 中暂时移除 `tabBar` 的 `iconPath` 和 `selectedIconPath`
- 或者在 `miniprogram/images/` 中添加图标文件

**问题2: 页面空白**
- 解决：检查 `app.json` 中的 `pages` 配置是否正确
- 确保页面文件存在

---

## ✅ 最终检查清单

### 数据库部分
- [ ] `最终完整数据库.sql` 已在Supabase执行
- [ ] 表结构创建成功
- [ ] 初始数据插入成功

### Web应用部分
- [ ] `npm install` 执行成功
- [ ] `npm run build` 执行成功
- [ ] `dist/` 文件夹已生成
- [ ] `download.html` 已创建
- [ ] 绿色鹦鹉图标已更新
- [ ] 应用名称已改为「言道学外语」

### APK部分
- [ ] Capacitor已初始化
- [ ] Android平台已添加
- [ ] Gradle镜像已配置
- [ ] APK打包成功
- [ ] `yandao.apk` 已生成

### 部署部分
- [ ] `dist/` 已上传到COS
- [ ] `download.html` 已上传到COS
- [ ] `yandao.apk` 已上传到 `download/` 目录
- [ ] 静态网站已配置
- [ ] 自定义域名已配置（可选）

### 小程序部分
- [ ] 小程序项目已导入
- [ ] 基本功能正常
- [ ] 准备上传审核（可选）

---

## 🎉 完成！

按照以上步骤操作，您的「言道学外语」项目就可以正式上线了！

### 项目特色总结
- 📚 4500+ 海量内容（笑话、童谣、小说）
- 🌍 10种语言学习
- 🤖 AI智能对话助手
- 🎮 5种学习游戏
- 🏆 成就系统和排行榜
- 👥 好友系统
- 📱 Android APP + Web版 + 小程序
- 🦜 可爱的绿色鹦鹉图标

### 如有问题
请参考各部分的详细说明，或联系技术支持。

---

**祝项目顺利上线！🚀**
